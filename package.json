{
  "name": "flaredrive-r2",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare R2 网盘：Pages Functions + 完整 WebDAV 服务端 + 无构建 Vue 前端",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler pages dev . --r2 BUCKET --persist-to .wrangler/state",
    "deploy": "wrangler pages deploy ."
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "wrangler": "^4.0.0"
  }
}
